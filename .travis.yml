language: node_js
node_js:
- '6'

notifications:
  email: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

env:
  - TRAVIS=travis CXX=g++-4.8

install:
  - 'npm install -g bower'
  - 'bower install'
  - npm install

script:
- npm test
